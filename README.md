## Commands

Set the project to use Node.js version v9.11.2 (npm v5.6.0)
```
nvm use
```

Build locally (without compression)
```
npx gulp
```

Build for deployment (with compression)
```
npx gulp prod
```